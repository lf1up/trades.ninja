import React from 'react';
import './Header.css';

const Header = () => (
  <header className="header">
    <div className="logo">trades.ninja</div>
    <button className="contact-button">Contact us</button>
  </header>
);

export default Header;
